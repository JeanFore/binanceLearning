{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": "npm run db:push && npm run db:seed",
    "startCommand": "npm run start"
  }
}
